for num in range(1, 10000):
    digits = [int(digit) for digit in str(num)]
    product = 1
    sum_digit = 0

    for digit in digits:
        product *= digit
        sum_digit += digit

    if product + sum_digit == num:
        print(num)
